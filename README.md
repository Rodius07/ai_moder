# TG Guard Bot

MVP Telegram-бота для модерации группового чата.

Что умеет:

- читает сообщения в группе;
- проверяет их по локальным правилам: стоп-слова, флуд, длина;
- локально распознает речь в voice/audio/video/video_note и учитывает ее как текст;
- опционально проверяет смысл сообщения через OpenAI;
- предупреждает по очевидным нарушениям и отправляет спорные случаи админам;
- отправляет спорные случаи в админ-чат;
- отвечает на вопросы через `/ask`;
- ищет в интернете для `/ask`, когда вопрос требует свежих данных;
- генерирует картинки через OpenRouter по `/image`;
- генерирует видео через OpenRouter по `/video`;
- показывает реквизиты поддержки проекта по `/donate`.

Бот никогда не кикает, не банит пользователей из чата и сам не удаляет сообщения.
Он может выдать предупреждение, временно ограничить отправку сообщений или отправить
случай админам. Удаление остается только ручной кнопкой для админа.

## Быстрый старт

```bash
python3.11 -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
cp .env.example .env
```

Заполните `.env`:

- `TELEGRAM_BOT_TOKEN` - токен от `@BotFather`;
- `ADMIN_CHAT_ID` - куда присылать спорные сообщения, можно оставить пустым;
- `OPENAI_API_KEY` - ключ OpenAI, можно оставить пустым для работы без ИИ.
- `OPENAI_BASE_URL` - альтернативный OpenAI-compatible endpoint, например
  `https://openrouter.ai/api/v1` для OpenRouter.
- `CHAT_RULES_PATH` - путь к уставу чата, по умолчанию `chat_rules.md`.
- `CONTEXT_MESSAGE_LIMIT` - сколько последних сообщений давать ИИ для понимания
  контекста, по умолчанию `10`.
- `DATA_PATH` - где хранить статистику и настройки чата.
- `ENABLE_LOCAL_TRANSCRIPTION` - включить локальное распознавание речи в аудио/видео.
- `WHISPER_MODEL_SIZE` - размер локальной модели, по умолчанию `base`.
- `MAX_TRANSCRIPTION_FILE_MB` - максимальный размер медиа для расшифровки.

Для OpenRouter:

```env
OPENAI_API_KEY=ваш_openrouter_key
OPENAI_BASE_URL=https://openrouter.ai/api/v1
OPENAI_MODEL=openai/gpt-5-mini
OPENAI_MODERATION_MODEL=google/gemini-2.0-flash-lite-001
OPENROUTER_APP_NAME=TG Guard Bot
OPENROUTER_IMAGE_MODEL=google/gemini-2.5-flash-image
OPENROUTER_VIDEO_MODEL=x-ai/grok-imagine-video
OPENROUTER_VIDEO_DURATION=5
```

Запуск:

```bash
tg-guard-bot
```

или:

```bash
python -m tg_guard_bot.main
```

## Важная настройка Telegram

Чтобы бот видел все сообщения в группе, добавьте его в группу администратором. Для удаления сообщений нужны права `Delete messages`, для мута - `Restrict members`.

Если бот не админ, нужно отключить privacy mode у `@BotFather`, но для модерации все равно обычно нужны админ-права.

## Команды

- `/start` - короткая справка;
- `/rules` - текущие базовые правила;
- `/settings` - настройки контекста `/ask`, модерации и молчащих;
- `/stats` - статистика нарушений и разжатость дня;
- `/ask вопрос` - задать вопрос ИИ;
- `/image промпт` или `/img промпт` - сгенерировать картинку через OpenRouter;
- `/video промпт` или `/vid промпт` - сгенерировать видео через OpenRouter;
- `/appeal` ответом на страйк - пересмотреть спорное решение;
- `/report` ответом на сообщение - попросить бота проверить нарушение;
- `/donate` - реквизиты поддержки бота;
- `/warns` - посмотреть свои предупреждения.

Полезные настройки в чате:

- `/settings webmode auto` - умный web-поиск только для свежих/поисковых вопросов;
- `/settings webmode openrouter` - всегда давать модели OpenRouter web search tool;
- `/settings webmode local` - DuckDuckGo + выдержки страниц;
- `/settings webmode off` - выключить интернет для `/ask`;
- `/settings modmodel google/gemini-2.0-flash-lite-001` - дешевая модель для проверки каждого сообщения;
- `/settings model openai/gpt-5-mini` - мощная общая модель для `/ask`, `/report`, `/appeal`;
- `/settings image google/gemini-2.5-flash-image` - модель картинок;
- `/settings image black-forest-labs/flux.2-pro` - пример альтернативной модели.
- `/settings video x-ai/grok-imagine-video` - модель видео.

`/ask`, `/image` и `/video` понимают явный запрос контекста: например,
`/image сделай мем по последним 15 сообщениям`. Для изменения картинки или
первого кадра видео ответьте командой `/image` или `/video` на сообщение с фото.

## Как работает модерация

1. Бот сохраняет последние сообщения чата в короткую историю.
2. Если сообщение содержит голосовое, аудио или видео, бот скачивает файл,
   локально распознает речь и добавляет расшифровку в историю как текст.
3. Локальный движок ищет очевидные технические нарушения: спам, флуд,
   стоп-слова.
4. Если подключен OpenAI, бот всегда просит ИИ классифицировать текущее сообщение
   с учетом устава из `chat_rules.md` и последних 10 сообщений чата.
5. Если сообщение признано нарушением, бот убирает его из будущего AI-контекста,
   чтобы следующие сообщения этого человека не считались плохими автоматически.
6. Если OpenAI не подключен или недоступен, бот использует локальный движок как
   запасной режим.
7. Если есть `ADMIN_CHAT_ID`, спорные решения уходят админам с кнопками.

Первый запуск распознавания речи скачает локальную модель Whisper в кеш. После этого
аудио и видео распознаются локально, без отправки медиафайлов во внешний API.

## Тесты

```bash
pytest
```
